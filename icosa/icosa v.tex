% !TEX encoding = UTF-8 Unicode
% !TEX program = xelatex

\documentclass{article}
	\usepackage[margin = 1in]{geometry}
	\usepackage{xeCJK}
		\setCJKmainfont{NotoSansTC-Regular}
	\usepackage{tikz}
	\usepackage{tikz-3dplot}
	\usepackage{listings}
\begin{document}

	\vspace*{2in}
	\fontspec{SourceCodePro-Regular}
	\lstset{
		language=[latex]tex, tabsize=4,
		moredelim=*[s][\itshape]{$}{$},
		moredelim=*[s][\color{red!40!.}]{(}{)},
		moredelim=*[s][\color{green!30!.}]{[}{]},
		backgroundcolor=\color{blue!5},
		commentstyle=\color{.!80}\itshape,
		texcsstyle=*\color{blue!40!.},
		moretexcs={
			draw, foreach
		},
		deletetexcs={},
	}
	\lstinputlisting{icosa.tex}

	\tikz[overlay] \path (5in, 6.5in) node {\includegraphics{icosa.pdf}};

\end{document}